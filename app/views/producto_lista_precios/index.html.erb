<h1>Listing producto_lista_precios</h1>

<table>
  <tr>
    <th>Producto</th>
  </tr>

<% @producto_lista_precios.each do |producto_lista_precio| %>
  <tr>
    <td><%=h producto_lista_precio.producto_id %></td>
    <td><%= link_to 'Show', producto_lista_precio %></td>
    <td><%= link_to 'Edit', edit_producto_lista_precio_path(producto_lista_precio) %></td>
    <td><%= link_to 'Destroy', producto_lista_precio, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New producto_lista_precio', new_producto_lista_precio_path %>