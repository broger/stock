<h1>Listing marca_categorias</h1>

<table>
  <tr>
    <th>Integer</th>
    <th>Integer</th>
  </tr>

<% @marca_categorias.each do |marca_categoria| %>
  <tr>
    <td><%=h marca_categoria.integer %></td>
    <td><%=h marca_categoria.integer %></td>
    <td><%= link_to 'Show', marca_categoria %></td>
    <td><%= link_to 'Edit', edit_marca_categoria_path(marca_categoria) %></td>
    <td><%= link_to 'Destroy', marca_categoria, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New marca_categoria', new_marca_categoria_path %>