<h1>Listing menus</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Parent</th>
    <th>Posicion pila</th>
    <th>Url</th>
    <th>Tooltip</th>
    <th>Ayuda</th>
  </tr>

<% @menus.each do |menu| %>
  <tr>
    <td><%=h menu.nombre %></td>
    <td><%=h menu.parent_id %></td>
    <td><%=h menu.posicion_pila %></td>
    <td><%=h menu.url %></td>
    <td><%=h menu.tooltip %></td>
    <td><%=h menu.ayuda %></td>
    <td><%= link_to 'Show', menu %></td>
    <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
    <td><%= link_to 'Destroy', menu, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New menu', new_menu_path %>